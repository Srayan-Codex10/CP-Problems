#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

int main()
{

    vector<int> keyb = {3, 1};
    vector<int> drives = {5, 2, 8};
    int b = 10;

    sort(keyb.begin(), keyb.end());
    sort(drives.begin(), drives.end());

    

    return 0;
}